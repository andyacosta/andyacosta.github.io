import style from './style';

const s = Object.create(style);

s.intro = {
	fontFamily: 'Open Sans, sans-serif',
	fontWeight: '800',
	fontSize: '70px',
	textAlign: 'center',
};

s.introBody = {
	marginBottom: '30px',
	paddingTop: '5px',
}

export default s;
