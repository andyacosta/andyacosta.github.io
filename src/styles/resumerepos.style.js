import style from './style';

const s = Object.create(style);

s.repoText = {
  'fontFamily' : 'Gentium Book Basic',
  'fontStyle' : 'italic',
  'fontSize' : '36px',
};

s.resumeText = {
  'fontFamily' : 'Open Sans',
  'fontSize' : '16px',
}

export default s;
